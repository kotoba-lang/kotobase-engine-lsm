{:paths ["src"]
 :deps {
        ;; kotoba.lang.text, not clojure.string. Pinned as an explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, so without a floor here
        ;; this repo silently rides whatever a sibling happens to name.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
       io.github.kotoba-lang/kotobase-engine-contract
        {:git/sha "7aced98bbe1a3faa8324c8e9161a83db767068e4"}
        ;; This engine requires ipld.core/value directly. Keep the runtime
        ;; portability boundary explicit instead of inheriting an arbitrary
        ;; transitive revision from an index implementation.
        io.github.kotoba-lang/io-ipld
        {:git/sha "48ba40490cf082a1473986ac757b8e2b214a3d1b"}
        io.github.kotoba-lang/merkle-lsm
        {:git/sha "401b79985ca081b497c3cc3ddfb241c4f0ab04fe"}
        io.github.kotoba-lang/prolly-tree
        {:git/sha "3a12dd2b77430007031e408ee8db75d35f1757a1"}
        io.github.kotoba-lang/kotobase-storage
        {:git/sha "fce6991cd1f0ff028b234317f2fdb2cfbd7b56ac"}
        ;; Reader half of ADR-2608060500. `decode-node` is the identity on
        ;; every block written so far, so this changes no behaviour today —
        ;; it makes this engine able to read a compressed run block, which
        ;; has to be true everywhere BEFORE merkle-lsm starts writing them.
        io.github.kotoba-lang/kotobase-block-codec
        {:git/sha "3ec88f11e7b35efd7f592f52c511a067d50bf096"}}
 :aliases
 {:test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]}
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts ["-m" "clj-kondo.main" "--lint" "src" "test"
                     "--fail-level" "error"]}}}
